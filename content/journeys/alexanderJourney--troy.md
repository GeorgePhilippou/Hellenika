---
_ignore: true
_reason: >
  Orphaned artefact from the migration script's filename scheme
  changing from id-based to key-based (to resolve the babylon/babylon
  id collision — see alexanderJourney--babylon.md). The live content
  for this stop is in alexanderJourney--alexander-*.md (filed by its
  `key`). Kept rather than deleted only because this filesystem
  mount doesn't permit deleting files.
---
