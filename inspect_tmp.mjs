/* Scratch file used once during the content-migration audit to inspect
   data/*.js field shapes (see scripts/content-schema.mjs for the result).
   Not part of the build or any script; kept empty rather than deleted
   because this filesystem mount doesn't permit deleting files. Safe to
   remove by hand. */
